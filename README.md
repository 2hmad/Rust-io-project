# Minigrep project with Rust

It is a command line used for search for a word / phrase in a file **( Ex: .txt file )** and extract its place in the file .

# Example code

```
cargo run bog poem.txt
```
